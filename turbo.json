{
    "$schema": "https://turborepo.org/schema.json",
    "tasks": {
      "build": {
        "dependsOn": ["^build"],
        "outputs": [".next/**", "dist/**"],
      },
      "dev": {
        "cache": false
      },
      "lint": {},
      "env": ["STRIPE_SECRET_KEY", "STRIPE_WEBHOOK_SECRET"]
    }
  }